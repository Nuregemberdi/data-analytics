# Прогресс журналы

**Версия: 20 · Акыркы жаңылоо: 2026-09-13**

> **Claude үчүн:** ар бир сессиянын башында ушул файлды оку. Эң акыркы жазуу — менин
> азыркы абалым. Жогорудагы версия номерин мага айт — ошондо экөөбүз бир эле
> көчүрмөнү карап турганыбызды билем. Версия эски болсо, кайра жаңылап оку.

**Азыркы абал:** 4-жума · Фаза 1 · SQL
**Максат:** дата аналитик, февраль 2027
**Жумасына:** 15–20 саат
**Репозиторий:** https://github.com/Nuregemberdi/data-analytics

> **Даталар жөнүндө:** алдын ала дата жазылбайт. Жума башталганда чыныгы датасы
> жазылат, бүткөндө аяктаган датасы кошулат. Себеби темп өзгөрөт — 1-жума бир
> күндө бүттү, башка жума эки жумага созулушу мүмкүн. Календарь эмес, мазмун
> башкарат.

---

## Шаблон (ар жума ушундай толтурулат)

```
## N-жума · күн–күн айы · Фаза X
**Максат:** (бул жумада эмнени бүтүрөм)
**Иштеген убакыт:** X саат

### Эмне бүттү
-

### Эмне түшүнбөй калдым
-

### Кийинки жумада
-

### Ишембиде кайра жазган тапшырма
-
```

---

## ✅ 1-жума · 1-сентябрь 2026 · Фаза 0 — БҮТТҮ

**Максат:** репозиторий, куралдар, биринчи commit
**Иштеген убакыт:** ~2 саат
**Эскертүү:** бир күндө бүттү — план боюнча бир жума берилген эле. Графиктен алдыдамын.

### Эмне бүттү
- [x] GitHub аккаунт жана ачык `data-analytics` репозиторийи
- [x] Git 2.52.0 — `clone`, `add`, `commit`, `push`
- [x] Python 3.14.0 (`py` аркылуу иштейт)
- [x] VS Code 1.135.0
- [x] pandas 2.3.3, numpy, matplotlib, seaborn, jupyter
- [x] Папкалар түзүлдү: `01-sql/ 02-python/ 03-stats/ 04-projects/ notes/ claude/`
- [x] Файлдар: `README.md`, `SETUP.md`, `PROGRESS.md`, `.gitignore`, `notes/errors.md`, `notes/terms.md`
- [x] Биринчи commit push кылынды — 11 файл
- [x] `.gitignore` текшерилди: `claude/` папкасы GitHub'ка чыккан жок ✅

### Эмне түшүнбөй калдым
- `python --version` иштебей, `py --version` иштеди.
  **Себеби:** Windows'тун "App execution alias" деген жасалма `python.exe`
  файлы (`AppData\Local\Microsoft\WindowsApps\`) чыныгы Python'ду жаап турат.
  **Чечим:** азырынча `py` колдоном. Кийин Settings → Apps →
  Advanced app settings → App execution aliases бөлүмүнөн өчүрсөм болот.
- DBeaver жана PostgreSQL орнотула элек — 4-жумада керек болот.

### Кийинки жумада
- 2-жума: SQL негизи — `SELECT`, `WHERE`, `ORDER BY`, `LIMIT`, `DISTINCT`, `NULL`. 30 маселе.

### Ишембиде кайра жазган тапшырма
- (1-жумада жок — 2-жумадан башталат)

---

## ✅ 2-жума · 1-сентябрдан башталды · Фаза 1 — SQL негизи — БҮТТҮ

**Максат:** `SELECT`, `WHERE`, `ORDER BY`, `LIMIT`, `DISTINCT`, `NULL` логикасы. 30 маселе.
**Ресурс:** https://sqlbolt.com — 1–6 сабактар (браузерде, эч нерсе орнотуунун кереги жок)
**Иштеген убакыт:** 8 саат

### Эмне бүттү
- [x] sqlbolt 1-сабак: `SELECT` жана `FROM` (5/5, week-02-basics.sql ичинде)
- [x] sqlbolt 2-сабак: `WHERE` — татаалыраак шарттар
- [x] sqlbolt 3-сабак: текст менен чыпкалоо (`LIKE`, `IN`)
- [x] sqlbolt 4-сабак: `ORDER BY`, `LIMIT`
- [x] sqlbolt 5-6-сабактар: кайталоо (5-сабак бүттү; 6-сабак = JOIN, 4-жумага калтырылды)
- [x] `NULL` эмне экенин өз сөзүм менен жаздым (`week-02-basics.sql` ичинде)
- [x] 30 маселе чечилди → 30/30
- [x] Ар бир суроого чейин "күтөм:" саптары толтурулду (1.1–1.5тен башка — алар эреже киргизилгенге чейин жазылган)

### Эмне түшүнбөй калдым
- `SELECT` менен `WHERE`тин айырмасы: кайсынысы катарларды азайтат?
  Бүгүн жаңылдым, кайра кайталашым керек.

    ✅ Чечилди: SELECT тилкелерди кесет, WHERE катарларды.

### Кийинки жумада
- 3-жума: `GROUP BY`, `HAVING`, агрегаттар. Фазанын эң маанилүү жумасы. 40 маселе.

### Ишембиде кайра жазган тапшырма
-


---

## ✅ 3-жума · 3–6-сентябрь 2026 · Фаза 1 — GROUP BY жана агрегаттар — БҮТТҮ
**Максат:** `COUNT`, `SUM`, `AVG`, `MIN`, `MAX`, `GROUP BY`, `HAVING`. 40 маселе.
**Ресурс:** https://sqlbolt.com — 6-сабактан ары (JOIN'ди кошпой)
**Файл:** `01-sql/week-03-groupby.sql`
**Иштеген убакыт:** 12 саат

### Эмне бүттү
- [x] `COUNT(*)` жана башка агрегаттар түшүнүлдү (COUNT, SUM, AVG, MIN, MAX)
- [x] `GROUP BY` — тизмени топторго бөлүү
- [x] `HAVING` менен `WHERE`тин айырмасын өз сөзүм менен жаздым (week-03-groupby.sql ичинде)
- [x] `COUNT(*)` менен `COUNT(тилке)` айырмасы: экинчиси **non-null** маанилерди гана санайт.
      Айырмасы = ошол тилкедеги NULL'дардын саны. $B \subseteq A \Rightarrow$ `COUNT(*)` ≥ `COUNT(тилке)`
- [x] Топтордун саны = топтолгон тилкенин **cardinality**'си (`COUNT(DISTINCT тилке)`)
- [x] $\text{SUM} = \text{AVG} \times \text{COUNT}$ — жаңы query жазбай, бар натыйжалардан
      жаңы санды чыгарууну биринчи жолу колдондум (5.7, Engineer = 17)
- [x] `WHERE` менен маани бирөө калганда `SELECT`теги топтолбогон тилке эмне үчүн
      коопсуз экенин талдадым — коопсуздук эреже боюнча эмес, кокустан
- [x] **Өз алдынча суроо түзүүнү баштадым** (5.8). Үлгү: «Кайсы [ТОП] боюнча
      [АГРЕГАТ] [ШАРТ]?» Текшерүү: шартты бир катарды карап текшере аламбы?
      ооба → `WHERE`, жок → `HAVING`
- [x] `ROUND` чыпкага да, кийинки эсепке да кирбейт — эң акырында гана
- [x] Бирдей орточо ≠ бирдей топ. `range` айырмалай албады (экөө тең 6),
      `variance` айырмалады (4.4 менен 6.0). `range` эки санды гана колдонот
- [x] **base rate** — салыштыруудан мурун топтун көлөмүн кара
- [x] **Чекти маалыматтан тандоо:** `HAVING`сиз аткар → сандарды кара →
      бөлүүчү санды тап. Чек өтө төмөн болсо баары өтөт, өтө жогору болсо эч ким
- [x] Ат сураган суроого `GROUP BY` жарабайт → `ORDER BY` + `LIMIT`
- [x] `HAVING`те alias эмес, туюнтма — SQLite кабыл алат, PostgreSQL алмайт
- [x] Alias эмнени өлчөсө, ошону айтат: `experience_range`, `career_span`,
      `length_range` (`snake_case`)
- [x] Ноль катар — ката эмес, жооп болушу мүмкүн
- [x] **Өз алдынча 8 суроо түздүм** (5.8–5.15)
- [x] 40 маселе чечилди → **40/40** ✅
- [x] Ар бир суроого чейин "күтөм:" саптары толтурулду

### Эмне түшүнбөй калдым
- **Суроо түзүү** — 3-жуманын эң алсыз жери болуп чыкты. Даяр тапшырманы чечүү
  оңой, бирок «кайсы топ, кайсы агрегат, кайсы чек» деп өзүм чечиш кыйын.
  Түзгөн суроолорумдун көбү эки мааниде окулуп калды же `HAVING`ти талап кылган
  жок. 4-жумада да машыгуу керек.
- Подзапрос — «эң узун фильм кимдики» деген суроого керек болду.
  Азырынча `ORDER BY` + `LIMIT` менен айланып өттүм, 4-жумада үйрөнөм.
- `HAVING` менен `WHERE`ти билем, бирок `HAVING`сыз чечүү жолун (`WHERE` + агрегат)
  өз алдынча тандай албай жүрөм — суроодо айтылганда гана эстейм.

### Кайталанган алсыз жерим (бул сессияда)
- Суроого жооп бербей, коңшу суроого жооп берүү — **3 жолу**
- Жыйынтык туура, жүйө жалган — **2 жолу** (`COUNT(building)`, `SUM` менен салыштыруу)
- `ROUND`дуу санды кийинки эсепке киргизүү — **1 жолу**
- Бардыгы `notes/errors.md` файлында жазылды

### Кайталанган алсыз жерим (3-жумада)
- Суроого жооп бербей, коңшу суроого жооп берүү — **13 жолу**
- Катар менен тилкени чаташтыруу — **9 жолу**
- Бир сөздүк жооп берүү — **12 жолу**
- Жыйынтык туура, жүйө жалган — **3 жолу**
- Баары `notes/errors.md` файлында, толук жазуулар менен

### Кийинки жумада
- 4-жума: `INNER`/`LEFT JOIN`, подзапрос, fan-out. 30 маселе.
- **Шарт:** PostgreSQL менен DBeaver орнотулушу керек — SQLBolt `JOIN` үчүн
  жетишсиз. Бул 1-жумадан бери калып келе жаткан иш.

### Ишембиде кайра жазган тапшырма
- `3.3` — «2 млндон ашкан шаарлары бирден көп болгон өлкөлөр» — файлды карабай,
  эстен нөлдөн жазылды (`R.1`, `week-03-groupby.sql` ичинде).
  **Query туура чыкты** — `WHERE` менен `HAVING`ти туура жерге койдум.
  **Божомол туура эмес** — 7–9 катар дедим, 1 чыкты, а чеги 4 эле.
  Сабак: `GROUP BY`даган тилкенин cardinality'си катарлардын эң жогорку чеги.
- Шарт: 40/40 бүтмөйүнчө 4-жумага өтпөйм.

---

## ✅ 4-жума · 7–13-сентябрь 2026 · Фаза 1 — JOIN жана подзапрос — БҮТТҮ 30/30

**Максат:** `INNER`/`LEFT JOIN`, подзапрос, fan-out. 30 маселе.
**Иштеген убакыт:** ~8 саат (эки күн)
**Эсеп: 19/30**

### Эсепке кирүү ченеми (ушул сессияда макулдашылды)

- **Кирет:** тапшырманы өзүм жазсам жана туура болсо. Тилкенин атын сурап алуу — кирет.
- **Кирбейт:** мугалим жообун берсе, же кадам сайын жетелесе.

Ушул ченем боюнча 2026-09-07де чечилген `1.1` эсепке кирген жок — жетелөө менен
жазылган эле. 2026-09-08де аны жетелөөсүз кайра жаздым, ошондо гана 1/30 болду.

### Эмне бүттү

**Курал (1-жумадан бери калып келе жаткан иш):**
- [x] PostgreSQL 17.11 (порт 5432) жана DBeaver Community 26.2.0 орнотулду
- [x] pgexercises клуб базасы **өз компьютериме** орнотулду: `exercises` базасы, `cd` схемасы
- [x] Текшерүү: `SELECT COUNT(*) FROM cd.members;` → 31

**JOIN түшүнүктөрү:**
- [x] `INNER JOIN` — эки таблицада тең дал келгендер гана
- [x] `LEFT JOIN` — сол таблица толук калат, дал келбегендери `NULL`
- [x] «Сол» — экрандагы орду эмес, **жазуу тартиби**: `FROM`дон кийинкиси сол,
      `JOIN`дон кийинкиси оң. `LEFT JOIN`до тартип маанилүү, `INNER JOIN`до эмес
- [x] **`fan-out`** — оң таблицада бир катарга бир нече дал келүү болсо, сол
      таблицанын катары көбөйөт. Кичине мисалда сан менен көрсөтүлдү
- [x] **`Cartesian product`** — `ON`суз ар бир катар ар бир катар менен жупташат.
      Өз мисалымда 4 × 2 = 8 деп өзүм эсептеп чыктым
- [x] **`ON` vs `WHERE` — 4-жуманын эң маанилүү сабагы.** `ON`догу шарт JOIN учурунда
      иштейт жана `LEFT JOIN`ду сактайт; `WHERE`деги шарт JOIN бүткөндөн кийин иштейт
      жана `LEFT JOIN`ду **`INNER JOIN`го айландырып коёт**.
      Муну эксперимент менен өзүм таптым — шартты `ON`дон `WHERE`ге көчүрүп, үч
      катардын жоголгонун көрдүм
- [x] `alias` — суралбай туруп өзүм колдоно баштадым (`m`, `b`)

**NULL жана үч маанилүү логика:**
- [x] `NULL` — белгисиздик, ноль эмес. Ноль — билинген маани
- [x] **`three-valued logic`**: `TRUE` · `FALSE` · `UNKNOWN`. `NULL > 4` = `UNKNOWN`
- [x] `WHERE` `TRUE` болгондорду **гана** калтырат → `UNKNOWN` түшүп калат.
      Дал ушул себептен `LEFT JOIN` + `WHERE` айкалышы `INNER JOIN`го айланат
- [x] `IS NULL` / `IS NOT NULL` — `NULL`ду кармаган жалгыз курал
- [x] `COUNT(*)` катарларды, `COUNT(тилке)` `NULL` эмес маанилерди санайт
- [x] Баасы жок топко `COUNT` **0** берет, `AVG`/`MIN`/`MAX` **`NULL`** берет.
      Себеби: саноо — билинген жооп; ал эми сумма ÷ 0 аныкталбайт

**Агрегат жана топтоо (3-жумадагы билим JOIN менен айкалышты):**
- [x] `SUM`, `MIN`, `MAX`, `ROUND(маани, белги)` кошулду
- [x] **Агрегат бар → `GROUP BY` керек. Агрегат жок → `GROUP BY` зыян**
- [x] `GROUP BY`га эки тилке (`surname`, `firstname`) жазуунун себеби: клубда үч
      `Smith` бар, жалгыз фамилия боюнча топтосо үчөө бир топко кошулуп калат
- [x] `>=` — `= 2 OR > 2` дегендин ордуна
- [x] **`LIMIT` кыркат, `COUNT` санайт** — экөө башка нерсе. `LIMIT` койгондо
      жалпы сан көрүнбөй калат
- [x] `ORDER BY ... DESC` менен `LIMIT` — тизменин аягын көрүү ыкмасы

**Аналитикалык (жаңы деңгээл):**
- [x] **`silent failure`ди үч башка түрдө көрдүм:**
      1. `ON`суз JOIN — 9 катар тынч чыгат, ката жок
      2. `GUEST` (`memid = 0`) чыпкаланбаса, 883 брондоо менен «эң активдүү мүчө»
         болуп тизменин башына чыгат
      3. Базада **эки Darren Smith** бар — бири 261 брондоо жасаган, экинчиси бир
         да жолу келген эмес. Ат боюнча издесе экөө бир адам болуп кетет
- [x] **`observation window`** — база 2012-07-03тен 09-30га чейинки үч айды гана
      камтыйт. `MIN(starttime)` берген нерсе «мүчө биринчи келген күн» эмес,
      «маалыматтагы эң эрте брондоо». Маани терезенин чегине жабышып турса,
      ал чыныгы маани эмес, чектин өзү болушу мүмкүн
- [x] **Божомолду query менен текшерүү рефлекси** — эки жолу өз алдынча колдондум:
      «Table Tennis ал күнү брондолгонбу?» жана «GUEST чын эле алындыбы?».
      Экөөндө тең божомолду ырастоочу өзүнчө query жаздым

### Чечилген маселелер (19/30)

| № | Тапшырма | Куралдары |
|---|---|---|
| 1 | David Farrell'дин брондоолору (pgexercises 1) | `INNER JOIN`, `WHERE ... AND` |
| 2 | Tennis courts, 2012-09-21 (pgexercises 2) | `LIKE`, күн аралыгы, `ORDER BY` |
| 3 | Massage Room 1 брондоолору | `INNER JOIN`, `WHERE` |
| 4 | Ар бир эмерек канча жолу брондолгон | `GROUP BY`, `COUNT`, `DESC` |
| 5 | Ар бир эмерек канча саат | `SUM(slots)/2` |
| 6 | 30дан ашык брондогон мүчөлөр | `HAVING`, GUEST чыпкасы |
| 7 | Ар бир мүчөнүн орточо `slots`у | `AVG`, `ROUND` |
| 8 | Бир да брондоосу жок мүчө | `LEFT JOIN` + `IS NULL` |
| 9 | Ар бир мүчөнүн биринчи брондоосу | `MIN` |

Ошондой эле 2026-09-07де өз таблицаларымда (`students`/`grades`) 15 query жазылган.
Алардын көбүндө калып көрсөтүлгөн, ошондуктан ченем боюнча кирбейт. Калыпсыз
жазылган **төртөө** гана эсепке кирди:

| № | Тапшырма | Куралдары |
|---|---|---|
| 10 | Айбектин гана баалары | `WHERE` ат боюнча |
| 11 | Ошол эле, `id` боюнча — оңдоону өзүм таптым | `primary key` менен издөө |
| 12 | Эки же андан көп баасы барлар | `HAVING`, `>=` |
| 13 | Орточосу 3төн төмөн **же** такыр жок окуучулар | `HAVING ... OR ... IS NULL` |

Акыркысы эң маанилүү: `IS NULL`сыз баасы такыр жок окуучулар — эң тобокелдүүлөрү —
отчётко таптакыр кирбей калмак.

**2026-09-09де кошулгандар:**

| № | Тапшырма | Куралдары |
|---|---|---|
| 14 | Окуучу жана насаатчысы | `self join`, `INNER` |
| 15 | Ошол эле, насаатчысы жоктор да | `self join`, `LEFT` |
| 16 | Бардык мүчө жана аны сунуштаган (pgexercises 4) | `self join` чыныгы базада |
| 17 | Tennis courts + брондогон адам (pgexercises 5) | **үч таблицалуу `JOIN`** |
| 18 | Орточодон төмөн маяна алгандар | **подзапрос**, `AVG` |
| 19 | Massage Room 1'ди брондогондор, `JOIN`сиз | подзапрос + `IN` |

### 2026-09-09 · жаңы темалар

- **`self join`** — бир таблица эки роль ойнойт, ошондуктан эки alias керек.
  Элестетүү: тизмени эки жолу басып чыгардың, бир кагазда «окуучулар»,
  экинчисинде «насаатчылар»
- **Үч таблицалуу `JOIN`** — ар бир `JOIN`дун өз `ON`у бар. Эки `JOIN` тең
  **fact** таблицага (`cd.bookings`) кошулат; `members` менен `facilities`
  бири-бирине кошулбайт, жалпы тилкеси жок
- **Подзапрос** — query'нин ичиндеги query. Ичкиси биринчи иштейт.
  `WHERE`де агрегат колдонуу үчүн жалгыз жол
- **Бир маани → `=` `>` `<`. Көп маани → `IN`.** Аралаштырса ката берет
- **Текшерүү ыкмасы:** кашаанын ичиндеги query өз алдынча иштей алат —
  бөлүп алып аткарып көр
- **⚠️ `DISTINCT` зыян болушу мүмкүн:** эки Darren Smith'ти бир катарга кысып,
  31 мүчөнү 30 кылып койду. `DISTINCT` кайталанууну «оңдобойт» — чыныгы
  айырманы да жашырат

### 2026-09-09 (экинчи сессия) · `NOT IN`

**Жаңы курал: `NOT IN`.** `IN` — тизмеде БАР болгондорду калтырат, `NOT IN` —
тизмеде ЖОК болгондорду. Тизме эки жерден келет: колдон жазылган (`facid NOT IN (0,1,2,3)`)
же подзапростон (`memid NOT IN (SELECT memid FROM cd.bookings)`).

**Эки шарт:** подзапрос **бир гана тилке** кайтарышы керек, жана ал тилке тышкы
тилке менен бир эле нерсени билдириши керек (`memid` — `memid`, `facid` — `facid`).

**`JOIN` менен айырмасы:** `JOIN` катарларды **кошот**, подзапрос **чыпкалайт гана**.
Ошондуктан подзапростун ичинде 629 катар болсо да, тышкы натыйжа көбөйбөйт.

**20-тапшырма** (брондоо жасабаган мүчөлөр, `NOT IN` менен) жазылды, туура иштеди,
8-тапшырманын жообу менен дал келди. **Бирок эсепке кирген жок** — мен сураганда
мугалим `okuuchular`/`sabak` мисалында дал ошол түзүлүштү толук чечип берди,
демек калып көрсөтүлдү. **Эсеп 19/30 бойдон.**

**Токтогон жер:** 21-тапшырма — `recommendedby` тилкесинде `NOT IN` (NULL тузагы).
Жазыла элек.

### 2026-09-11 · `NOT IN`, `NULL` тузагы, `COUNT(DISTINCT)`, нормалдаштыруу

> ⚠️ Бул бөлүм бир жолу жазылып, файл эски абалына кайтып калган (VS Code / git).
> Кайра жазылды. **Жазгандан кийин версия номерин текшер.**

**Эки күн тыныгуу болду** (09-09 кечинен 09-11ге чейин). Арада SQL жазылган жок.

**Бүгүн жети маселе: 20 → 26.** Эсеп **26/30**, төрт маселе калды.

| № | Тапшырма | Куралдары |
|---|---|---|
| 20 | Эч кимди сунуштабаган мүчөлөр | `NOT IN` + `IS NOT NULL` |
| 21 | Эң көп адам сунуштаган мүчө | `self join`, `GROUP BY`, `COUNT` |
| 22 | Massage Room 1ди эң көп брондогон 5 мүчө | `GROUP BY`, `ORDER BY`, `LIMIT` |
| 23 | Ар бир эмерек боюнча канча башка мүчө | **`COUNT(DISTINCT)`** |
| 24 | Эки массаж бөлмөсүнүн баасы | божомолду текшерүү |
| 25 | Эки бөлмө: брондоо жана мүчө саны | эки агрегат бир query'де |
| 26 | Ар бир эмерек боюнча бир мүчөгө канча брондоо | **нормалдаштыруу**, `::numeric` |

---

#### 1. `NOT IN` + `NULL` тузагы

`WHERE recommendedby IS NOT NULL` сабын **өзүм коштум** — тузак алдын ала айтылган жок.

Чынжыры: `memid = 5`, тизме `(3, 7, NULL)` болсун.

```
5 != 3    → TRUE
5 != 7    → TRUE
5 != NULL → UNKNOWN     (белгисиз сан 5 болуп калышы мүмкүн)
------------------------------------
TRUE AND TRUE AND UNKNOWN → UNKNOWN
```

`WHERE` **`TRUE` болгондорду гана** өткөрөт → **0 катар**, ката билдирүүсүз.

**`UNKNOWN` жугуштуу:** бир `UNKNOWN` бүт `AND` чынжырын `UNKNOWN` кылат.
**Эреже:** `NOT IN` ичинде ар дайым `IS NOT NULL`. `IN`де бул тузак жок.

#### 2. `tie-breaker`

`LIMIT 5` койгондо 5 жана 6-орун бирдей болсо, тандоо **`non-deterministic`** болот.
Чечими: `ORDER BY`га экинчи тилке кошуу — `ORDER BY count DESC, m.memid`.
`tie-breaker` **уникалдуу** (`primary key`) болушу керек, антпесе теңдикти бузбайт,
жөн гана кийинкиге жылдырат. `surname` жарабайт — эки Darren Smith бар.

#### 3. `COUNT(DISTINCT тилке)`

`COUNT(b.memid)` катарларды санайт, `COUNT(DISTINCT b.memid)` **башка маанилерди**
санайт. `SELECT DISTINCT`тен айырмасы: бул бир гана тилкенин ичинде иштейт,
натыйжадан катар өчүрбөйт.

#### 4. `integer division` — бүгүнкү эң чоң сабак

`round(count(...)/count(...))` жаздым → **17.0** чыкты, а кол менен эсептегенде 17,54.
**Мен өзүм байкадым:** «калдык жок, жана туура эмес тегеректеген».

Себеби: `count()` бүтүн сан кайтарат, ал эми **бүтүн ÷ бүтүн = бүтүн**. Ондук бөлүгү
тегеректелбейт, **кыркылат**. `ROUND`го 17,54 жеткен эмес — ага 17 барган.

Чечими — **`type casting`**: `count(...)::numeric / count(...)`, же `CAST(... AS numeric)`.
Жана `ROUND`го экинчи аргумент: `ROUND(..., 2)`.

#### 5. Нормалдаштыруу — менин алсыз жеримдин так ордун толтурат

Massage Room 2 башкалардан эки эсе аз мүчө тарткан. Үч божомол айттым:
VIP, жаңы, тейлөө сапаты. **Текшерилчүсү бирөө гана** — баа (`testable hypothesis`).
Текшердим: эки бөлмөнүн баасы **бирдей** → божомол **жокко чыкты** (`falsified`).
Бул жеңилүү эмес — бир түшүндүрмө четке кагылды.

Андан кийин чыныгы айырма чыкты: брондоолор **421 менен 27**. Бир мүчөгө:
**17,54 менен 2,25**.

- Эки топту түз салыштырууга болбойт, себеби **өлчөмү** башка
- Жалпы негизге бөлүү — **`normalization`**, чыккан сан — **`rate`**
- Математикада бөлчөктөрдү жалпы бөлүмгө келтирүү менен бирдей ой
- 421 vs 27 «популярдуулук» дейт; 17,54 vs 2,25 «кайра келүү» дейт — эки башка суроо

#### 6. `sample size` — ченемди жалгыз көрсөтпө

Бир адам 20 жолу кошумча брондосо:

| Эмерек | Мурун | Кийин |
|---|---|---|
| Massage Room 2 | 2,25 | **3,92** (+74%) |
| Pool Table | 29,04 | **29,78** (+2,5%) |

Кичине сандын үстүндөгү ченем **туруксуз**.
**Эреже: ченемдин жанында ар дайым `n` турушу керек.**

#### 7. Метод жана өзүм жөнүндө

- **Өз жообумду коргодум.** GUEST жөнүндөгү сынга: «тапшырмада андай шарт коюлган
  эмес» дедим — мугалим макул болду. Кесиптик ченем: жоопту өзгөртпөйсүң,
  **эскертүү** кошосуң. Чечим сурагандыкы
- **Мугалимге эки сын айттым:** (1) тапшырма жүрүп жатканда «жооп бербедиң» деп
  кыстарбасын, андай эскертүүлөр сессиянын аягына чогултулсун; (2) **бир билдирүүгө
  бир суроо** — «мен адаммын». Экөө тең кабыл алынды
- **Мотивация жөнүндө сүйлөштүк.** Программанын **16%ы** бүткөн (60/375 саат).
  Календарь боюнча графиктен эки жума алдыдамын. Мугалимдин диагнозу: 10 күн бою
  машыгуу гана, көрүнгөн натыйжа жок. Маалыматты Оштун чыныгысына алмаштыруу
  сунушталды — мен pgexercises менен улантууну тандадым

### 2026-09-11 (кечки уландысы) · коррелденген подзапрос

**Эсеп 26 → 27.** Үч маселе калды.

#### Коррелденген подзапрос деген эмне

Мурунку подзапростор бир жолу иштеп, бир жооп берчү — ичкиси тышкысы жөнүндө эч
нерсе билчү эмес. **Коррелденген подзапрос ичинде тышкы queryдин тилкеси турат**,
ошондуктан ал тышкы querydin **ар бир катары үчүн кайра** иштейт.

Математикалык аналогия: мурункусу — **турактуу сан**, коррелденгени — **функция
`f(x)`**: ар бир `x` үчүн өз мааниси.

Белгиси: ички queryде тышкы alias турат, демек **эки alias милдеттүү** (`b` жана `b2`).
Баасы: жай иштейт — 4044 катар болсо, ички query 4044 жолу аткарылат.

#### Эң маанилүү эки түшүнүк

**1. Байланыш сабы `GROUP BY`дын ордун ээлейт.**
27-тапшырманы жазганда адегенде `b2.memid = b.memid` деп жаздым, аткарып көрүп
өзгөрттүм. Айырмасы:

- `b2.memid = b.memid` → ар бир **мүчөнүн** эң узагы
- `b2.facid = b.facid` → ар бир **эмеректин** эң узагы

Query'нин калган бөлүгү бирдей, суроо таптакыр башка. **Ката чыкпайт.**

**2. Тышкы query тандабайт — чыпкалайт.**
Тышкы query агрегат колдонбойт жана `GROUP BY` да керек эмес. Ал ар бир катарды
карап: «менин `slots`ум ошол максимумга барабарбы?» деп сурайт. Тандоону ички query
жасайт. Ошондуктан `GROUP BY` бир топтон **бир сан** берсе, коррелденген подзапрос
**толук катарды** кайтарат — катардагы башка тилкелери менен кошо.

#### 27/30 · Ар бир эмеректин эң узак брондоосу

```sql
SELECT f.name, m.firstname, m.surname, b.slots
FROM cd.bookings b
INNER JOIN cd.members m ON m.memid = b.memid
INNER JOIN cd.facilities f ON b.facid = f.facid
WHERE b.memid != 0
  AND b.slots = (
        SELECT MAX(b2.slots)
        FROM cd.bookings b2
        WHERE b2.facid = b.facid
          AND b2.memid != 0
  );
```

Түзүлүштү толук өзүм жаздым, жетелөө болгон жок.

#### ⚠️ Жаңы `silent failure`: чыпка бир гана queryде

Акыркы сап (`AND b2.memid != 0`) адегенде жок эле. Натыйжада тогуздун ордуна
**беш эмерек** чыкты, ката билдирүүсүз.

Себеби: ички query GUEST'ти кошуп эсептейт → төрт эмеректе эң узак брондоо GUEST'тики
→ тышкы query GUEST'ти алып салган → ошол санга барабар мүчө жок → **эмерек тизмеден
таптакыр жоголот**.

| Эмерек | Эң узагы | Ким |
|---|---|---|
| Squash Court | 14 | GUEST |
| Tennis Court 1 | 12 | GUEST |
| Massage Room 1 | 8 | GUEST |
| Massage Room 2 | 4 | GUEST |

**Муну да өзүм таптым** — тышкы чыпканы алып салып, GUEST'тин катарларын көрүп.
Бүгүн экинчи жолу экспериментке салып себеп таптым.

**ЭРЕЖЕ: чыпка эки queryде тең болушу керек.** Бирөөндө бар, экинчисинде жок болсо —
экөө эки башка суроого жооп берип калат.

#### Теңдик — отчётко эскертүү

Оңдолгон натыйжада Massage Room 1 боюнча **тогуз катар** бар, баары 4 slots.
Бул ката эмес, чыныгы теңдик. Ченем: жашырба, отчётко жаз — «эң узагы 4 slots,
аны тогуз мүчө бөлүшөт». Бирөөнү тандоо — сурап жаткандын чечими.
Теңдиктен бирөөнү автоматтык тандоо — 5-жуманын темасы (`ROW_NUMBER`).

#### Бир ката: `DISTINCT` рефлекси кайра кайтты

«Эмеректердин бири жок» деген көйгөйгө «демек `DISTINCT` керек» деп жооп бердим.
`DISTINCT` кайталанууга карата рефлекс болуп калыптыр, ал эми маселе кайталанууда
эмес, **жоголууда** эле. 09-09дун сабагы толук бекиген жок.

### 2026-09-11 (кечки уландысы 2) · подзапрос `FROM`до

**Эсеп 27 → 29.** Бир маселе калды.

#### `derived table` — подзапрос `FROM`до

`FROM`догу подзапрос **убактылуу таблица** жаратат. Аты — `derived table`.
Query иштеп бүткөнчө жашайт, базада сакталбайт.

Эки эреже:

1. `AS` менен **ат берүү милдеттүү** — PostgreSQL атсыз кабыл албайт
2. Тышкы query аны кадимки таблица катары карайт; тилкелеринин аттары —
   подзапростун `SELECT`инде `AS` менен берилгендер

Эмнеге керек: **агрегаттын үстүнөн агрегат**. `AVG(COUNT(...))` деп жазууга болбойт.
Адегенде `COUNT`ту убактылуу таблицага чыгарасың, анан анын үстүнөн `AVG` аласың.

#### 28/30 · Бир мүчөгө орточо канча брондоо туура келет

Жообу **109.00**. Кол менен текшердим: мүчөлөрдүн брондоолору 3161
(26-тапшырманын таблицасынан), брондоо жасаган мүчөлөр 29 (31 мүчөнүн бири GUEST,
дагы бири эч качан брондоо жасаган эмес). 3161 ÷ 29 = 109. Дал келди.

#### 29/30 · Эмеректер боюнча мүчө санынын эң чоңу жана эң кичинеси

```sql
SELECT MAX(t.unique_members) AS max,
       MIN(t.unique_members) AS min
FROM (
    SELECT facid, COUNT(DISTINCT memid) AS unique_members
    FROM cd.bookings
    WHERE memid != 0
    GROUP BY facid
) AS t;
```

27 жана 12 — 23-тапшырманын таблицасы менен дал келди.

**Бул тапшырма атайын текшерүү катары берилген.** 28ди жазгандан кийин мугалим аны
машыгуу деп эсептеди (калып көрсөтүлгөн). Мен макул болгон жокмун. Ортодон чечтик:
түзүлүш айтылбаган жаңы тапшырманы жардамсыз жазсам — экөө тең эсепке кирет.
Жаздым. **Экөө тең кирди.**

Бир эскертүү: биринчи вариантымда GUEST чыпкасы жок эле — так ошол кечээки сабак
(«чыпка эки queryде тең») кайра кайталанды.

#### 30/30 · Орточо ченемден төмөн эмеректер — ЖАЗА АЛБАДЫМ

Жообу берилди, ошондуктан эсепке кирген жок.

```sql
SELECT t.name, t.rate
FROM (
    SELECT f.name,
           ROUND(COUNT(b.bookid)::numeric / COUNT(DISTINCT b.memid), 2) AS rate
    FROM cd.bookings b
    INNER JOIN cd.facilities f ON b.facid = f.facid
    WHERE b.memid != 0
    GROUP BY f.name
) AS t
WHERE t.rate < (
    SELECT AVG(t2.rate)
    FROM (
        SELECT ROUND(COUNT(b.bookid)::numeric / COUNT(DISTINCT b.memid), 2) AS rate
        FROM cd.bookings b
        WHERE b.memid != 0
        GROUP BY b.facid
    ) AS t2
)
ORDER BY t.rate;
```

Орточо ченем ≈ 14,71. Андан төмөн бешөө: Badminton Court, Tennis Court 1,
Tennis Court 2, Squash Court, Massage Room 2.

**Эмне үчүн эсептөө эки жолу жазылды:** `AS t` деген ат **тышкы queryде гана** жашайт.
`WHERE`дин ичиндеги подзапрос көз каранды эмес query, ал `t`ны көрбөйт. Ошондуктан
ошол эле эсептөө `t2` деген ат менен кайра жазылды.

Бул кооз эмес жана коркунучтуу: бир жагын оңдоп, экинчисин унутсаң — ката чыкпайт,
жалган жооп чыгат. **Ошол ыңгайсыздык `CTE`ди жаратты** — 5-жуманын биринчи темасы.

#### Курал: DBeaver форматтоо

`Ctrl + Shift + F` — query'ни автоматтык иретке келтирет.
Жөндөө: `Окно → Настройки → Редакторы → Редактор SQL → Форматирование`,
ал жерден `Регистр ключевых слов` → `Верхний`. Query'леримде `select`/`SELECT`
аралашып жүргөндүктөн керек болду.

#### Деңгээл жөнүндө суроо бердим: «бул тапшырмалар так junior'бу?»

Мугалимдин жообу:

| Тапшырма | Деңгээл |
|---|---|
| 20–23 (`NOT IN`, `COUNT(DISTINCT)`, `self join`) | Junior, маектин так ортосу |
| 24–26 (божомол, нормалдаштыруу, `::numeric`) | Junior, аналитикалык жагы күчтүүрөөк |
| 27 (коррелденген подзапрос) | Junior/Middle чеги |
| 30 (эки катмарлуу `derived table`) | Junior'дан жогору; `CTE` менен — кадимки |

SQLде дагы жок нерселерим: `CTE`, терезе функциялары, `CASE WHEN`,
күн-убакыт функциялары (`DATE_TRUNC`, `EXTRACT`), `UNION`, текст функциялары.

**Эң маанилүү эскертүү:** «кодуң junior деңгээлине жакын, сүйлөгөнүң — али эмес».
Маекте query жазуу менен катар аны **сөз менен түшүндүрүү** суралат
(«эмне үчүн `LEFT JOIN`?», «бул query качан жалган жооп берет?»).
Бүгүн ошондой суроолорго төрт жолу бир сүйлөм менен жооп бердим.

### 2026-09-13 · 4-ЖУМА БҮТТҮ · 30/30

#### 30/30 · Орточодон көп брондогон мүчөлөр

```sql
SELECT
    m.firstname,
    m.surname,
    mb.booking_count
FROM (
    SELECT memid, COUNT(*) AS booking_count
    FROM cd.bookings
    WHERE memid != 0
    GROUP BY memid
) mb
JOIN cd.members m ON m.memid = mb.memid
WHERE mb.booking_count > (
    SELECT AVG(counts.cnt)
    FROM (
        SELECT COUNT(*) AS cnt
        FROM cd.bookings
        WHERE memid != 0
        GROUP BY memid
    ) counts
)
ORDER BY mb.booking_count DESC;
```

14 мүчө чыкты. Эң көбү Tim Rownam — **408 брондоо** (орточосу 109).

#### Эң маанилүү эпизод: түшүнбөгөн курулманы алып салуу

Биринчи вариантымды чыгара албай, **теория карадым** жана `CROSS JOIN` колдонуп
жаздым. Query туура иштеди. Мугалим байкады: «`CROSS JOIN`ду биз өткөн жокпуз,
кайдан келди?»

Теория кароо ката эмес — чыныгы жумушта документация окуу күнүмдүк иш. Тыюу салынганы
даяр жоопту көчүрүү. Бирок бир шарт коюлду: **түшүндүрө албаган сапты кодуңда
калтырба** — маекте так ошол сап жөнүндө сурашат.

Мен `CROSS JOIN`ду алып салып, өзүм билген куралга (`WHERE`деги подзапрос)
алмаштырып кайра жаздым. Мугалим муну кесиптик рефлекс деп атады.

**`CROSS JOIN` деген эмне экен:** ал — мен мурунтан билген **`Cartesian product`**,
жөн гана башка ат менен. 09-08де 4 × 2 = 8 деп өзүм эсептеген элем. `ON` шарты жок,
себеби ал сол таблицанын ар бир катарын оң таблицанын ар бир катары менен жуптайт.
Менин учурумда оң жагында **бир гана катар** бар эле (орточо сан), ошондуктан катар
саны өзгөргөн жок — ошол себептен коопсуз болгон.

#### 4-ЖУМАНЫН ЖЫЙЫНТЫГЫ

| Өлчөм | Абал |
|---|---|
| 4-жума | **30/30 ✅** |
| Жалпы маселе | 2-жума 30 + 3-жума 40 + 4-жума 30 = **100** |
| Фаза 1дин 1-критерийи (100 маселе) | **АТКАРЫЛДЫ** |

Калган эки критерий:
- Терезе функциясын жардамсыз жазуу — 5-жумада
- **Башканын туура эмес query'син таап оңдоо** — сынак али коюла элек

#### 4-жумада өтүлгөн куралдардын толук тизмеси

`INNER JOIN` · `LEFT JOIN` · `self join` · үч таблицалуу `JOIN` ·
`fan-out` · `Cartesian product` / `CROSS JOIN` · `ON` vs `WHERE` ·
подзапрос `WHERE`де (`=`, `IN`, `NOT IN`) · `NOT IN` + `NULL` тузагы ·
`three-valued logic` · коррелденген подзапрос · подзапрос `FROM`до (`derived table`) ·
`COUNT(DISTINCT)` · `tie-breaker` · `integer division` жана `::numeric` ·
нормалдаштыруу (`rate`) · `sample size`

#### 4-жумада өзүм тапкан үч нерсе (мугалим айткан жок)

1. `integer division` — «калдык жок, жана туура эмес тегеректеген»
2. GUEST'тин `MAX(slots)` тузагы — тышкы чыпканы алып салып текшердим
3. Коррелденген подзапростун байланыш сабы (`memid` → `facid`) — аткарып көрүп оңдодум

### Эмне түшүнбөй калдым

- **`self join`** — өтүлө элек. Эки жолу баштаганда токтотуп, «бул теманы өтө элек
  болгонубуз үчүн токтоттук элего» деп өзүм эскерттим. Кийинки сессиянын биринчи темасы.
- **Подзапрос** — 4-жуманын планында бар, али башталган жок.
- **Синтаксисти эстеп калуу** — `LEFT JOIN` + `IS NULL` тапшырмасында төрт жолу
  тыгылдым: `FROM`го туура эмес таблица, `IS NULL`дун ордуна `!= 0`, эки alias катар.
  Логиканы билем, кол эсине албайт. Кайталоо керек.
- **Таблицанын тилкелерин эстебейм** — бир сессияда таблицаны 10 жолу кайра сурадым.
  Ошол себептен эки такта жасалды (JOIN тактасы жана клуб базасынын картасы),
  экрандын жарымында ачык турат.

### Кайталанган алсыз жерим (бул жумада)

- Суроого жооп бербей, коңшу суроого жооп берүү — **20дан ашык**
- Бир сөздүк жооп берүү — **20дан ашык**
- **«Же/же» суроосуна «ооба» деп жооп берүү — 5 жолу** (жаңы, өзүнчө белгиленди)
- Түшүнүктүн атын атай албоо — **6 жолу**: `Cartesian product`, `primary key`,
  `IS NULL`. Инстинкт бар, аты жок
- Тилкелерди чаташтыруу — **4 жолу**: `gid` vs `grade`, `slots` vs `starttime`
- Баары `notes/errors.md` файлында

### Методго киргизилген өзгөрүү (өзүм сынадым)

Мугалимге эки сын айттым, экөө тең кабыл алынды:

1. **Темадан темага секирүү.** pgexercises ар бир тапшырмада жаңы нерсе кошот —
   бир түшүнүктү эми түшүнүп чыгар, а башкасы башталат. Чечим: мен билген
   куралдар менен гана өз машыгууларым түзүлөт, жаңы курал бир гана кошулат.
2. **`LIMIT` койгондо «канча катар чыкты?» деп сураш туура эмес** — `LIMIT` жалпы
   санды жашырат. Мугалим макул болду.

### Кийинки жумада

- `self join` — нөлдөн, кичине мисал менен
- Подзапрос
- 30 маселени толтуруу (дагы 21 калды)

### Ишембиде кайра жазган тапшырма

- `1.1` (David Farrell) — 2026-09-07де жетелөө менен чечилген эле, ошондуктан
  эсепке кирген жок. 2026-09-08де жетелөөсүз кайра жаздым, 34 катар туура чыкты.
  **Шарт өзүм койдум:** жалган прогресс керек эмес.
